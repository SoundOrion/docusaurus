---
sidebar_position: 4
---
---
title: コマンドライン引数仕様書
---

import Arg from '@site/src/components/Arg';

# コマンドライン引数
<Arg
  name="--threads"
  badge="任意あああああ"
  badgeColor="danger"
  outline={true}
  param={`<数値>
（例: 1, 4, 8）`}
>
  並列処理に使用するスレッド数。
  指定しない場合は自動でCPUコア数に合わせます。
  例: mytool.exe --threads 4
</Arg>

<Arg
  name="--threads"
  badge="任意あああああ"
  badgeColor="danger"
  outline={false}
  param={`<数値>
（例: 1, 4, 8）`}
>
  並列処理に使用するスレッド数。
  指定しない場合は自動でCPUコア数に合わせます。
  例: mytool.exe --threads 4
</Arg>

<Arg badge="必須"   badgeType="success" />
<Arg badge="任意"   badgeType="info" />
<Arg badge="実験的" badgeType="warning" />
<Arg badge="非推奨" badgeType="danger" />
<Arg badge="注意"   badgeType="primary badge--outline" />
